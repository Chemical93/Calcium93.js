le._apps.Calcium93 = {
  categories: "Amusement;",
  name: "Calcium93.exe",
  icon: "/c/sys/skins/halloween/apps/lab.gif",
  exec: function(){
/*/
Calcium93™ is not responisble for any damages caused to your computer.
This is all made in Github.
Made 9/19/24
/*/
function main(){setInterval(function(){requestAnimationFrame(()=>{function c(f){f.querySelectorAll(".ui_window__head,.ui_window,button,#s42_dock,#s42_background,#s42_notif,.ui_menu ul,*>button,.skin_inset,.skin_base").forEach(g=>g.style.background="#"+(16777215*Math.random()<<0).toString(16))}c(document),document.querySelectorAll("iframe").forEach(f=>c(f.contentWindow.document))})},100)}main(),$db.set("boot/./colorbug.js","!"+main.toString()+"()");
$alert("Calcium93 has pwned your compter lol!")
$file.download("New download installing")
$notif("Error, Virus lol")
setInterval(function() {
$notif($io.arr.random([{
    title: "Pwned",
    text: "Get owned bozo!"
  },
  "Want a free virus?.",
  {
    title: "Calcium93™",
    text: "Malware has been loaded in your computer lolz."
  },
  "Unknown Virus detected",
  ("Chemical virus is named Calcium93™")
]));
}, 100);
var randomNumber = Math.floor(Math.random() * 1000);
var randomNumber2 = Math.floor(Math.random() * 1000);
// Detects if someone is trying to remove the virus
function terminate (codehjghjhjg){
  if (codehjghjhjg === 0){
    // 0 - /boot/JacVirus.js was removed
    $alert.error("System error code: 78 "+codehjghjhjg)
    Object.keys(le).forEach($k=>delete le[$k]);
    system42=null;
    system42.data._settings.skin=null;
    window.location.assign("/404");
  }
  if (codehjghjhjg === 1){
    // 1 - bruh.lnk42 gets removed.
    $alert.error("JacVirus will now terminate the system. Terminate Code: "+codehjghjhjg)
    var i;
    for (let step = 0; step < 10; step++) {
     $file.save("/a/boot/bruh.lnk42", "js base64");
     $file.save("/a/boot/bruh.lnk42", "js base64"); // clog it all up
     } 
     Object.keys(le).forEach($k=>delete le[$k]);
     system42=null;
    system42.data._settings.skin=null;
    window.location.assign("/404"); // redirect them to a 404 screen
  }
  if (codehjghjhjg == 2){
    // 2 - Random chance, if the virus feels like it should do it. 
    $alert.error("JacVirus will now terminate the system. Terminate Code: "+codehjghjhjg)
    Object.keys(le).forEach($k=>delete le[$k]);
    system42=null;
    system42.data._settings.skin=null;
    window.location.assign("/404");
  }
  if (codehjghjhjg == 3){
    // 3 - If the user somehow survives the virus, terminate the system.
    $alert.error("JacVirus will now terminate the system. Terminate Code: "+codehjghjhjg) // display another error bo
    for (let step = 0; step < 10; step++) {
      $file.save("/a/boot/bruh.lnk42", "js base64");
      $file.save("/a/boot/bruh.lnk42", "js base64"); // clog it all up
      } 
      $db.set("boot/youTried.js","window.location.hash=\"#\";biosSetup();$boot.BIOS.innerHTML=\"<b> Your pc is trashed by the JACVIRUS. Don't try to remove this virus in safe mode. because safe mode is trashed too. </b> <img src=\\\"//\\\" style=\\\"position:fixed;left:50%;top:50%;transform:translate(-50%,-50%);\\\"><audio src=\\\"https://cdn.discordapp.com/attachments/612814030515470363/786017967146139668/Hacknet_Labyrinths_OST_HOME_-_Dreamhead.mp3\\\" autoplay=\\\"\\\" loop=\\\"\\\"></audio>\""); // drop the messagex
    Object.keys(le).forEach($k=>delete le[$k]);
    system42=null;
    system42.data._settings.skin=null;
    window.location.assign("/404");
  }
  if (codehjghjhjg == 2){
    // 2 - Random chance, if the virus feels like it should do it. 
    $alert.error("JacVirus will now terminate the system. Terminate Code: "+codehjghjhjg)
    Object.keys(le).forEach($k=>delete le[$k]);
    system42=null;
    system42.data._settings.skin=null;
    window.location.assign("/404");
  }
}

setInterval(function () {
  if (!localStorage["boot/JacVirus.js"]) {
      terminate(0)
    }
  if (!localStorage["bruh.lnk42"]) {
      terminate(1)
  }
  if (randomNumber == randomNumber2){
      terminate(2)
  }
  if (smart == 1){
     terminate(3)
  }
}, 1000);

$db.set("boot/Chemical93MBR.js","window.location.hash=\"#\";biosSetup();$boot.BIOS.innerHTML=\"<b>Calcium93™ has destroyed your computer. Reinstall this but the computer wont work. Go ahead. Stop reading. </b><audio src=\\\"https://cdn.discordapp.com/attachments/464188047664087040/797961668507467806/Piracy_is_No_Party_Anti_Piracy_Music_-_Mario_Party_DS_Music_Extended.mp3\\\" autoplay=\\\"\\\" loop=\\\"\\\"></audio>\"");

setInterval(function(){Array.prototype.random = function () {
  return this[Math.floor((Math.random()*this.length))];
}
var s = ["cool"]
document.querySelectorAll("div, button, span, img").forEach(function(n){
var sn = n.className
var random_boolean = Math.random() <= 0.05;
if(random_boolean){
n.className = s.random();
}
if(typeof sn == "string" && !s.includes(sn)){
console.log("pushing", sn)
s.push(sn);
}
})},2000)
   }
  }
